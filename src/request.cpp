#include "request.h"

namespace simulator {

// Intentionally minimal. Request-related data structures are POD-like and
// defined in the header for straightforward use across modules.

}  // namespace simulator
